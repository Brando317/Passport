create table accesstokens(ID INT NOT NULL auto_increment primary key, userID INT NOT NULL, token text, expires datetime, _created timestamp default current_timestamp, constraint fk_userid foreign key (userid) references Users(id));

